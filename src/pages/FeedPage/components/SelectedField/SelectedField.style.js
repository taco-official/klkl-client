import styled from 'styled-components'
import { StyleSection } from '../../FeedPage.style'

const FieldSection = styled(StyleSection)`
  > div.alignEnd {
    align-self: flex-end;
  }
`

export default FieldSection
